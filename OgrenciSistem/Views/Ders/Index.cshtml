@model IEnumerable<OgrenciSistem.Models.tDers>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dersAd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dersAd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.dersID }) |
            @Html.ActionLink("Details", "Details", new { id=item.dersID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.dersID })
        </td>
    </tr>
}

</table>
